import { User } from '../../../components/users'
import { AccessToken, Client, RefreshToken } from '../../../components/authentication'

export const mainModels = {
  User,
  Client,
  AccessToken,
  RefreshToken
}
